<span class="name">
  <strong><%= link @user.human_name, to: user_path(@conn, :show, @user), class: "text-dark" %></strong>
  <%= link "@#{@user.handle}", to: user_path(@conn, :show, @user), class: "text-secondary" %>
</span>
